// Firebase config — paste the object from your Firebase console here.
// Until a real config is set, the interaction features (hearts, guestbook,
// view counter) stay hidden and the gallery works normally.
//
// It should end up looking like:
// window.FIREBASE_CONFIG = {
//   apiKey: "…", authDomain: "…", projectId: "…",
//   storageBucket: "…", messagingSenderId: "…", appId: "…"
// };
window.FIREBASE_CONFIG = null;
